interface:
  display_name: "Implementation Plan"
  short_description: "Inspect a repo and produce a concise implementation plan."
  default_prompt: "Use $implementation-plan to inspect this workspace and produce a concise implementation plan."
policy:
  allow_implicit_invocation: true
