interface:
  display_name: "Code Review"
  short_description: "Review changes for bugs, risks, regressions, and test gaps."
  default_prompt: "Use $code-review to inspect the current changes and report actionable findings."
policy:
  allow_implicit_invocation: true
