# Domain — Platform-Specific Testing

Want to contribute an agent? See the [contribution guide](../../docs/CONTRIBUTING.md).

This folder is flat — all domain agents sit directly here, no sub-folders per platform.
Agent names must include a platform prefix: `sap-*`, `salesforce-*`, `game-*`, `blockchain-*`, `aem-*`.
